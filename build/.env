# env vars for docker-compose.yml
# do not confuse with env vars for containers

# env vars for PRODUCTION configuration
# it is used when docker-compose.prod.yml is the first yml file

# 1 - start tor container, 0 - don't
TOR_PROXY=0
